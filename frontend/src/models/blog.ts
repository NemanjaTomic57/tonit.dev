export interface Blog {
    id: number;
    author: string;
    publicationDate: string;
    thumbnailSlug: string;
    heading: string;
    markdown: string;
    slug: string;
    hidden: boolean;
}
