services:
  frontend:
    build:
      context: ../frontend
      dockerfile: Dockerfile
    ports:
      - 3000:3000
    restart: always
    depends_on:
      - backend
  
  backend:
    build:
      context: ../backend
      dockerfile: Dockerfile
    ports:
      - 5010:5010
    restart: always